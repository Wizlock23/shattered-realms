{
  "name": "shattered-realms-mobile-alpha",
  "version": "0.8.1",
  "private": true,
  "description": "The Shattered Realms digital CCG \u2014 Windows/PWA Alpha",
  "scripts": {
    "ios:setup": "bash scripts/setup-ios.sh",
    "ios:sync": "npx cap sync ios",
    "ios:open": "npx cap open ios",
    "web:serve": "python3 -m http.server 8080 --directory www",
    "pwa:serve": "python -m http.server 8080 --directory www",
    "pwa:check": "node scripts/check-pwa.mjs"
  },
  "dependencies": {
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
